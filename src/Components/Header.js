function MyHeader() {
  return <div className="App-header">header: Let's learn React</div>;
}

// export const MyHeader = () => (  //  need to call by exact name
//   <div className="App-header">const header: Let's learn React</div>
// );

export default MyHeader; //  give a chance to call it by any name
